name: ged_structural_features
channels:
  - conda-forge
  - defaults
dependencies:
  # Python version
  - python=3.11
  
  # Core scientific computing
  - numpy>=1.26.0
  - scipy>=1.11.0
  
  # Graph processing
  - networkx>=3.1
  
  # Data manipulation
  - pandas>=2.2.0
  
  # Machine learning
  - scikit-learn>=1.4.0
  
  # Visualization
  - matplotlib>=3.8.0
  - seaborn>=0.13.0
  
  # Optimal Transport (install via pip)
  - pip
  - pip:
    - POT>=0.9.0
    - tqdm>=4.66.0
